image: node:latest

cache:
  paths:
  - node_modules/

job:
  script:
  - apt-get update -qy
  - apt-get install -y ruby ruby-dev rubygems-integration
  - npm install
  - gem install --no-rdoc --no-ri dpl
  - dpl --provider=heroku --Jada-s-Bae=Jada-s-Bae--api-key=1a532ca5-dfba-4285-bd2a-a92e091feb05
# change app name and api key if necessary
